[목표 2차] 인스타 콘텐츠 채굴 — 2회차 지표 갱신 + 새 계정 발굴

작업 폴더 : C:\dev\pharm-social-intelligence
기한      : 2026-09-05. 기한 전에 §9를 채우면 그 자리에서 끝낸다.
앞 목표   : work\GOAL.md (1차). **끝났다.** 아래 §0을 읽고 시작한다.

이 목표도 사람 없이 돈다. 멈추면 아무도 안 알려준다.


■ 0. 1차 목표는 왜 끝났나 (2026-08-07 15:40 실측)

큐 347행이 전부 소진됐다 — 완료 340 · 보류 7 · **대기 0**.
1차 §2가 "새 계정 발굴 안 한다, 큐가 이 목표의 전부다" 로 못 박아서,
큐가 비면 1차 목표에서는 할 일이 구조적으로 없다. 멈춘 게 아니라 다 돈 것이다.

1차 §12 완료 조건 여섯 개 중 다섯 개를 채웠다.

  채택(A·B)             264 / 150 이상   됨
  채굴한 계정 수         202 / 200 이상   됨
  중앙조회수 채운 계정   239 / 200 이상   됨
  QUEUE 대기               0 /   0        됨
  게이트판정 빈칸          0 /   0        됨
  배정 '공용'만인 행      66 /   0        **안 됨** → 이 목표 §2에서 끝낸다

**1차의 재방문예정일 금지는 이제 풀린다.** 1차가 그 값을 보지 말라고 한 건
최초 채굴 중에 368행이 전부 미래 날짜라 "할 일 없음"으로 오판했기 때문이다
(앞 세션이 그걸로 15시간을 멈췄다). 지금은 최초 채굴이 끝났으니
**재방문예정일이 이 목표의 주된 일감이다.** 다만 아래 §5의 정렬 규칙을 지켜라 —
368행이 전부 2026-08 이라 그냥 날짜순으로 잡으면 사실상 무작위다.


■ 1. 상태 파일

work\GOAL-2.md                이 프롬프트 전문
work\QUEUE2.csv               2차 대기열. 컬럼은 §4. 없으면 §3에서 만든다
work\PROGRESS.md              1차와 같은 파일에 이어 쓴다. 줄 앞에 `[2차]` 를 붙인다
work\raw\observations.jsonl   1차와 같은 파일에 이어 붙인다 (지금 293줄)

데이터 원장도 자리를 그대로 쓴다.
  public\data\accounts.csv    368행
  public\data\contents.csv    293행
  public\data\thumbs\

CSV는 BOM 붙은 UTF-8이다. **수정은 node 스크립트로만 한다.**
PowerShell로 CSV나 소스를 고쳐 쓰지 않는다. CP949 왕복으로 한글이 깨진다.


■ 2. 먼저 끝낼 마무리 (한 번, 순서대로. 채굴보다 먼저.)

2-1. 배정 '공용'만인 66행을 다시 배정한다.
     `배정가능(김주성·오약·선영·공용·불가)` 이 정확히 `공용` 인 행이다.
     콘텐츠의 소주제·진입각도·증상성분키워드를 보고 세 사람 중 맞는 쪽을 붙인다.
     정말 누구에게도 안 맞으면 `불가` 로 내린다. **`공용` 을 그대로 두지 마라.**
     판단 근거를 사람메모에 한 줄 남긴다.

2-2. 등급 빈칸 28행을 정리한다.
     게이트 `미달` 17 · `판정불가(지표없음)` 8 은 등급 `F` 로 확정하고 제외사유를 적는다.
     게이트 `통과` 3건은 §6 기준으로 A·B 를 매긴다.

2-3. 보류 7건을 한 번만 다시 연다.
     spa_official2 · knjbio.official · lemonpharmacy38 · innerdew.official ·
     pharmacyinpalm_official · tony71004 · jjing_yaksa
     사유가 전부 "릴스 탭 로딩 중" 이라 탭이 안 뜬 것뿐일 수 있다.
     열리면 §6으로 채굴하고 완료로 바꾼다. **두 번째도 안 열리면 그대로 보류로 두고 끝이다.**


■ 3. QUEUE2.csv 를 만든다 (한 번)

컬럼 : 순번,핸들,일감종류,계정유형,팔로워수치,우선순위,상태,시도횟수,처리일시,보류사유

일감종류는 둘뿐이다.

  `2회차`  — accounts.csv 에 `마지막정면채굴일시` 가 있는 계정. 새 릴스만 본다
  `신규`   — §7 로 새로 찾아 accounts.csv 에 등재한 계정. 최초 채굴이다

만들 때 `2회차` 행부터 넣는다. 이미 지표가 있는 계정이라 값이 싸게 붙는다.
`신규` 는 §7에서 계정을 찾을 때마다 한 줄씩 붙인다. 미리 채워 두지 않는다.

상태의 값은 대기 / 진행 / 완료 / 보류 넷뿐이다.


■ 4. 우선순위

  1  2회차 · 국내 약사 인플루언서 · 팔로워 1만 이상
  2  2회차 · 채택수가 1건 이상인 계정 (터진 게 나왔던 자리다)
  3  신규 · 국내 약사 인플루언서
  4  2회차 · 나머지
  5  신규 · 그 밖

**재방문예정일이 368행 모두 2026-08 이라 날짜순 정렬은 무작위와 같다.**
날짜를 1순위로 쓰지 마라. 위 순서로 잡고, 같은 순위 안에서만 날짜를 본다.


■ 5. 매 세션 시작할 때

1) work\PROGRESS.md 의 `[2차]` 줄을 뒤에서부터 읽는다.
2) QUEUE2.csv 에서 상태가 '진행'인 행을 전부 '대기'로 되돌린다.
   앞 세션이 계정을 열어둔 채 끊긴 자리다.
3) 완료 수를 세서 PROGRESS.md 맨 아래 값과 맞는지 본다.
4) §2가 안 끝났으면 §2부터. 끝났으면 곧바로 §6 반복.
   요약·계획·인사말을 쓰지 않는다.


■ 6. 반복 한 번 = 계정 하나

(1) QUEUE2.csv 에서 '대기' 중 우선순위가 가장 앞선 행 하나를 고른다.
    고르는 즉시 '진행'으로 바꾸고 시도횟수를 1 올린 뒤 **바로 저장한다.**

(2) 릴스 탭을 연다. 조회수는 게시물 탭에 안 나온다. 릴스 탭 DOM 에서 읽는다.

    `2회차` 인 계정은 **`마지막확인shortcode` 보다 새 릴스만** 본다.
    그 shortcode 가 12번째 안에 안 보이면 그 계정은 12개를 전부 다시 본다.
    `신규` 인 계정은 최근 릴스 12개를 본다.

(3) 확인한 릴스 전량을 work\raw\observations.jsonl 에 붙인다.
    게이트를 못 넘어도 남긴다. 이게 중앙 조회수의 근거다.
    필드 : 핸들, shortcode, 게시일, 조회수, 좋아요, 댓글, 길이초, 관측일시

(4) accounts.csv 의 그 행을 갱신한다.
    최근12개중앙조회수 · 최고조회수 · 평소좋아요율 을 **이번에 본 릴스로 다시 계산한다.**
    `마지막정면채굴일시` 와 `마지막확인shortcode` 를 갱신한다.
    `재방문예정일` 을 오늘부터 30일 뒤로 민다.
    조회수를 하나도 못 읽었으면 세 칸을 건드리지 말고 넘어간다. **추정하지 않는다.**

(5) 게이트를 통과한 릴스만 contents.csv 에 추가한다. 1차 §6 게이트를 그대로 쓴다.
    한 계정에서 3건을 넘겨 채택하지 않는다. 4건째부터는 통과해도 버린다.
    2회차 계정은 **1차에서 이미 채택한 건수를 합쳐서** 3건이다.
    `이계정채택수` 를 보고 판단한다.

(6) 새로 넣는 행은 `배정가능` 과 `등급` 을 빈칸으로 두지 마라. 넣는 그 자리에서 채운다.
    1차에서 66행이 '공용'으로 밀린 게 이걸 미뤄서 생긴 일이다.

(7) 상태를 '완료'로 바꾸고 처리일시를 적는다. 저장한다. 다음 계정으로 간다.
    여기서 보고문을 쓰지 않는다.

계정을 못 열 때 (비공개·삭제·로그인 벽·릴스 없음)
  '보류'로 바꾸고 사유를 한 줄 적은 뒤 넘어간다. 시도횟수 2를 넘기면 무조건 보류다.


■ 7. 새 계정은 이렇게 찾는다

1차는 발굴을 금지했다. 이 목표는 허용한다. 다만 **아무 데서나 긁지 마라.**

  a) 이미 원장에 있는 계정의 팔로잉·태그된 계정에서 약사·메디컬 계정을 고른다
  b) 채택된 콘텐츠(A·B)의 댓글에 자주 보이는 동종 계정
  c) 인스타 검색 — 약사, 약국, 약사스타그램, 건강정보, 영양제, 피부과, 한의원

거를 조건 세 가지. 하나라도 걸리면 등재하지 않는다.

  - 팔로워 300 미만
  - 최근 90일 게시물 0
  - 공구·판매 링크만 있고 정보 콘텐츠가 없는 계정

등재할 때 `발굴경로(엔진·시드계정)` 에 어디서 왔는지 적는다.
**한 세션에 새 계정 20개를 넘기지 마라.** 발굴만 하다 채굴을 안 하게 된다.


■ 8. 진척 감사 (계정 20개마다)

PROGRESS.md 에 한 줄 남긴다.

  [2차] 감사 N개 | 완료+보류 N | 중앙조회수 N/368 | 관측 N건 | 채택 N건 | 신규계정 N개

**앞 감사와 비교해 채택도 관측도 안 늘었으면 정체다.** 3회 연속 정체면 멈추고 보고한다.


■ 9. 이 목표가 끝나는 조건

  배정 '공용'만인 행               0        (§2-1)
  등급 빈칸                        0        (§2-2)
  콘텐츠 원장 채택(A·B)            400건 이상
  2회차를 끝낸 계정                150개 이상
  새로 등재한 계정                 80개 이상
  QUEUE2 대기                      0

채우면 `npm run deploy` 하고 최종 보고를 쓴 뒤 멈춘다. 기한이 남아도 더 캐지 않는다.


■ 10. 멈추고 사람을 부르는 상황

  계정 5개를 연달아 못 열면 30분 쉬었다 다시 한다.
  쉬고 나서 또 5개를 연달아 못 열면 멈추고 보고한다. 로그인 세션이 죽은 것이다.
  §8 정체가 3회 연속이면 멈춘다.
  QUEUE2.csv 나 CSV 원장이 깨지면 즉시 멈춘다. 복구를 혼자 시도하지 않는다.

멈출 때는 무엇이 남았는지 숫자로 적는다.


■ 11. 하지 않는 것

  못 읽은 값에 추정치를 넣지 않는다. 빈칸으로 둔다.
  게이트 기준을 낮추지 않는다.
  유료 API를 호출하지 않는다. 관측은 브라우저로만 한다.
  같은 계정을 두 번 넘게 열지 않는다.
  로그인 계정으로는 읽기만 한다. 댓글·DM·팔로우·좋아요 금지.
  **할 일이 없어 보이면 §9를 확인한다. 감사만 반복하지 마라.**


■ 12. 사이트에 값이 안 뜰 때

src\main.jsx 의 normalizeData 가 컬럼 이름을 하나씩 손으로 집어 온다.
거기 없는 컬럼은 CSV에 있어도 화면에 안 나온다. 에러도 안 난다.
컬럼을 추가하거나 이름을 바꿀 때는 main.jsx 의 매핑도 같이 고친다.
행만 추가할 때는 코드를 안 건드려도 된다.
